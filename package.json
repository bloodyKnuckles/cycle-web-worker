{
  "name": "cycle-web-worker",
  "version": "0.1.0",
  "author": "Jay Scott ANDERSON",
  "license": "MIT",
  "dependencies": {
    "@cycle/dom": "10.0.0-rc28",
    "@cycle/xstream-run": "2.0.0",
    "snabbdom": "^0.5.0",
    "xstream": "2.1.x"
  },
  "devDependencies": {
    "browserify": "13.0.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.4.3",
    "babelify": "7.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildindex": "browserify src/index.js -t babelify --outfile dist/index.js",
    "buildmain": "browserify src/main.js  -t babelify --outfile dist/main.js",
    "build": "npm run buildindex && npm run buildmain"
  }
}
